{
  "agent_task": "ERP Material Lifecycle Stabilization",
  "version": "1.0",
  "audit_date": "2026-03-07",
  "objective": "Fix all identified bugs and architectural gaps in the Flutter/Dart ERP system to make inventory, production, dispatch, and sales flows 100% reliable in both online and offline modes. Code inspection was already done. Now implement all fixes in priority order without breaking existing functionality.",

  "global_constraints": [
    "Do NOT modify any UI screens unless explicitly required by a fix.",
    "Do NOT change database schema without migration scripts.",
    "Each fix must be isolated and testable independently.",
    "All stock mutations MUST write a corresponding ledger entry.",
    "All remote writes MUST go through the durable outbox queue.",
    "Every fix must include a unit test or integration test verifying the fix."
  ],

  "tasks": [
    {
      "id": "T1",
      "ref": "R1",
      "severity": "CRITICAL",
      "title": "Fix department stock inconsistency between local and remote",
      "files": [
        "lib/services/inventory_service.dart"
      ],
      "problem": "transferToDepartment() and returnFromDepartment() do not change products.stock locally, but remote issue_to_department sync decrements products.stock. Department return has no remote sync path at all. This causes local vs server stock divergence.",
      "steps": [
        "Step 1: Define one invariant in a comment at top of inventory_service.dart: products.stock = main warehouse stock only. Department stock is tracked separately in departmentStocks.",
        "Step 2: In transferToDepartment() - decrement products.stock locally AND write a local ledger entry of type 'department_issue'.",
        "Step 3: In returnFromDepartment() - increment products.stock locally AND write a local ledger entry of type 'department_return'.",
        "Step 4: Add a new durable outbox command 'return_from_department' that mirrors the existing 'issue_to_department' command.",
        "Step 5: In the sync delegate, handle 'return_from_department' to increment products.stock on server and update server departmentStocks.",
        "Step 6: Write a unit test: issue 10 units to department, return 10 units, verify products.stock is unchanged net, departmentStocks is zero, and two ledger entries exist."
      ],
      "acceptance_criteria": "After issue + return cycle: local products.stock == server products.stock, departmentStocks[dept] == 0, ledger has both entries."
    },
    {
      "id": "T2",
      "ref": "R2",
      "severity": "CRITICAL",
      "title": "Block salesman stock allocation from sales flow",
      "files": [
        "lib/services/sales_service.dart"
      ],
      "problem": "Sales service allows recipientType == 'salesman' which moves stock to a salesman directly without dispatch workflow, route, or vehicle controls.",
      "steps": [
        "Step 1: In sales_service.dart, find the block where recipientType is checked.",
        "Step 2: Add a guard: if recipientType == 'salesman', throw a BusinessRuleException with message 'Salesman stock allocation must go through dispatch workflow. Use dispatch screen instead.'",
        "Step 3: Ensure this guard fires for both local write and before any queue enqueue.",
        "Step 4: Write a unit test that attempts to create a sale with recipientType='salesman' and asserts the exception is thrown and no stock is mutated."
      ],
      "acceptance_criteria": "Any attempt to create a sale with recipientType='salesman' throws immediately. No stock, ledger, or queue record is created."
    },
    {
      "id": "T3",
      "ref": "R4",
      "severity": "HIGH",
      "title": "Make warehouse stock partitioning real or remove multi-warehouse UI",
      "files": [
        "lib/services/opening_stock_service.dart",
        "lib/services/inventory_service.dart"
      ],
      "problem": "warehouseId is stored on records but all mutations still target one shared product.stock. Warehouse-wise inventory is fake metadata.",
      "steps": [
        "Step 1: Audit the codebase and answer: Is the business operationally single-warehouse right now? If YES, go to Step 2a. If NO, go to Step 2b.",
        "Step 2a (single-warehouse): Remove warehouse selector from opening stock UI. Set a default warehouseId = 'main' everywhere. Add a TODO comment: 'Multi-warehouse to be implemented in v2'. Skip remaining steps.",
        "Step 2b (multi-warehouse): Create a new Isar collection WarehouseStock { warehouseId, productId, stock }.",
        "Step 3b: Migrate all existing products.stock into WarehouseStock with warehouseId = 'main'.",
        "Step 4b: Update all stock read/write calls in inventory_service.dart to use WarehouseStock keyed by (warehouseId, productId).",
        "Step 5b: Update dispatch, sales, and production to pass warehouseId context and read/write from WarehouseStock.",
        "Step 6b: Write a test: add opening stock in warehouse A and warehouse B, dispatch from warehouse A, verify warehouse B stock is unchanged."
      ],
      "acceptance_criteria": "Either multi-warehouse UI is removed and system is cleanly single-warehouse, OR each warehouse has an independent stock value that is enforced in all mutations."
    },
    {
      "id": "T4",
      "ref": "R5",
      "severity": "HIGH",
      "title": "Fix opening stock to use set-balance semantics and prevent duplicates",
      "files": [
        "lib/services/opening_stock_service.dart",
        "lib/screens/inventory/opening_stock_setup_screen.dart"
      ],
      "problem": "Opening stock always adds to current stock. Saving the same row twice silently doubles stock. No uniqueness enforcement exists.",
      "steps": [
        "Step 1: Add a unique compound index on (productId, warehouseId) in the opening stock Isar collection.",
        "Step 2: Change saveOpeningStock() to use Isar's putByIndex (upsert) instead of put (append). This converts add semantics to set semantics.",
        "Step 3: Before Go-Live lock is applied, allow edits that overwrite previous value. After Go-Live, block further edits.",
        "Step 4: In opening stock setup screen, check for existing entry before save. If entry exists, show confirmation dialog: 'Opening stock for this product already exists. Do you want to update it?'",
        "Step 5: Write a test: save opening stock of 100 units, save again with 150 units for same product+warehouse, verify final stock is 150 not 250."
      ],
      "acceptance_criteria": "Saving opening stock for same product+warehouse twice results in one record with the latest value. No double-counting."
    },
    {
      "id": "T5",
      "ref": "R11",
      "severity": "HIGH",
      "title": "Standardize user identity key across all collections",
      "files": [
        "lib/services/sales_service.dart",
        "lib/screens/sales/new_sale_screen.dart",
        "lib/services/delegates/sales_sync_delegate.dart",
        "lib/services/delegates/inventory_sync_delegate.dart"
      ],
      "problem": "Some writes use AppUser.id, some sync filters use Firebase UID. Salesman records can disappear from sync scope or cause permission mismatches.",
      "steps": [
        "Step 1: Create a single helper function canonicalUserId() in lib/utils/auth_utils.dart that always returns Firebase UID.",
        "Step 2: Search entire codebase for AppUser.id usages in write and filter contexts.",
        "Step 3: Replace all such usages with canonicalUserId().",
        "Step 4: For existing records that used AppUser.id, add a one-time migration in app startup that reads old records and rewrites with Firebase UID.",
        "Step 5: Write a test: create a sale with a user whose AppUser.id != Firebase UID, sync, verify the sale appears in filtered pull."
      ],
      "acceptance_criteria": "All collections use Firebase UID as the user key. Sync pull filters and writes use the same value. No records are orphaned."
    },
    {
      "id": "T6",
      "ref": "R7",
      "severity": "HIGH",
      "title": "Move production and bhatti to durable outbox queue",
      "files": [
        "lib/services/production_service.dart",
        "lib/services/bhatti_service.dart",
        "lib/services/cutting_batch_service.dart"
      ],
      "problem": "Production flows use best-effort immediate sync. Local stock mutates but remote never receives the change during poor connectivity.",
      "steps": [
        "Step 1: Study how sales_service.dart enqueues to the durable outbox. Use that as the template.",
        "Step 2: In production_service.dart, after local Isar write succeeds, enqueue an outbox command 'production_log_create' with full payload.",
        "Step 3: In bhatti_service.dart, after local Isar write, enqueue 'bhatti_batch_create' and 'bhatti_batch_edit' commands.",
        "Step 4: In cutting_batch_service.dart, after local Isar write, enqueue 'cutting_batch_create' command.",
        "Step 5: In sync_queue_processor_delegate.dart, add handlers for the three new command types.",
        "Step 6: Remove or disable the old immediate sync calls in production, bhatti, and cutting services.",
        "Step 7: Write a test: create a production log while offline, come online, verify remote receives the command exactly once."
      ],
      "acceptance_criteria": "Production, bhatti, and cutting writes are queued durably. Retry and idempotency rules that apply to sales also apply to these flows."
    },
    {
      "id": "T7",
      "ref": "R3",
      "severity": "HIGH",
      "title": "Write local ledger entries for all customer and van sales",
      "files": [
        "lib/services/sales_service.dart"
      ],
      "problem": "Customer and van sales deduct allocated stock locally but create no local ledger entry. Offline reconciliation fails.",
      "steps": [
        "Step 1: Identify all code paths in sales_service.dart that deduct allocated stock for customer or van sales.",
        "Step 2: After each such deduction, write a StockLedger entry with: type='sale', saleId, productId, quantity (negative), timestamp, userId.",
        "Step 3: Do the same for sale edits (write a reversal + new entry) and sale reversals (write a reversal entry).",
        "Step 4: Write a test: create a customer sale offline, inspect local ledger, verify a negative quantity entry exists for each line item."
      ],
      "acceptance_criteria": "After any customer or van sale, local stockLedger has matching deduction entries. Ledger-based balance calculation matches allocated stock."
    },
    {
      "id": "T8",
      "ref": "R14",
      "severity": "HIGH",
      "title": "Require authenticated actor for production writes",
      "files": [
        "lib/services/production_service.dart"
      ],
      "problem": "Production log creation may continue without authenticated actor if supervisor payload context is present, allowing unauthenticated mutations.",
      "steps": [
        "Step 1: At the entry point of createProductionLog() (and any production mutation method), add: if (currentUser == null && supervisorContext?.signedToken == null) throw AuthenticationRequiredException('Production write requires authenticated user or signed supervisor context.').",
        "Step 2: Define AuthenticationRequiredException in lib/exceptions/.",
        "Step 3: If supervisorContext is used, validate the signed token against a known secret or server-issued value. Do not trust unsanitized field values.",
        "Step 4: Write a test: call createProductionLog() with null auth and populated supervisor fields containing unsigned data. Verify it throws."
      ],
      "acceptance_criteria": "Production writes require either a valid Firebase auth session or a cryptographically verified supervisor token. Null auth with plain supervisor fields is rejected."
    },
    {
      "id": "T9",
      "ref": "R10",
      "severity": "HIGH",
      "title": "Create centralized department master and normalize issue/return flow",
      "files": [
        "lib/screens/inventory/material_issue_screen.dart",
        "lib/screens/inventory/material_return_screen.dart"
      ],
      "problem": "Departments are hardcoded or ad hoc. Issue and return screens use different department lists. Material issued to a custom department cannot be found for return.",
      "steps": [
        "Step 1: Create a Department Isar collection: { id, name, isActive }.",
        "Step 2: Seed with existing hardcoded department names on first run.",
        "Step 3: In material issue screen, replace hardcoded list and free-text input with a dropdown that reads from Department collection.",
        "Step 4: In material return screen, use the same Department collection to populate the list.",
        "Step 5: All departmentStock records must reference department.id, not raw name string.",
        "Step 6: Write a test: issue material to a department created from master, open return screen, verify the same department appears in dropdown."
      ],
      "acceptance_criteria": "Issue and return screens share the same department source. No ad hoc department name can be entered. All department lookups use canonical ID."
    },
    {
      "id": "T10",
      "ref": "R6",
      "severity": "HIGH",
      "title": "Enforce BOM and yield validation in production service layer",
      "files": [
        "lib/services/production_service.dart",
        "lib/services/cutting_batch_service.dart",
        "lib/services/bhatti_service.dart"
      ],
      "problem": "Production services trust operator-entered quantities. No BOM or yield rule enforcement. Impossible yields can be posted as valid.",
      "steps": [
        "Step 1: Define a BomRule model: { productId, inputMaterialId, inputQtyPerUnit, minYieldPct, maxYieldPct }.",
        "Step 2: In createProductionLog(), before writing, load BomRule for the product. Verify: actualOutput <= (rawMaterialConsumed / inputQtyPerUnit) * maxYieldPct. If violated, throw BomViolationException with details.",
        "Step 3: Apply same check in bhatti and cutting services for their respective production types.",
        "Step 4: Expose BOM rules as admin-configurable records (not hardcoded).",
        "Step 5: Write a test: attempt production with 1 kg input and 1000 kg output. Verify BomViolationException is thrown."
      ],
      "acceptance_criteria": "Production entries with impossible or out-of-range yields are rejected at service layer before any stock mutation or ledger write."
    },
    {
      "id": "T11",
      "ref": "R8",
      "severity": "MEDIUM",
      "title": "Align cutting batch UI unit with service consumption unit",
      "files": [
        "lib/screens/production/cutting_batch_entry_screen.dart",
        "lib/services/cutting_batch_service.dart"
      ],
      "problem": "UI validates semi-finished stock in BOX. Service may consume Kg, Box, or fallback weight. Screen shows valid while service uses different quantity.",
      "steps": [
        "Step 1: In cutting_batch_service.dart, extract the stock resolution logic into a public method resolveStockPlan(productId, requestedQty) that returns { resolvedQty, resolvedUnit }.",
        "Step 2: In cutting_batch_entry_screen.dart, call resolveStockPlan() when the user selects a product. Display the resolved unit and quantity to the user before confirmation.",
        "Step 3: Pass the resolved values back to the service on confirmation. Service must use exactly these resolved values, not re-resolve.",
        "Step 4: Write a test: for a product with base unit Kg but box display, verify that UI shows Kg quantity and service consumes that exact Kg quantity."
      ],
      "acceptance_criteria": "UI displays the exact unit and quantity that will be consumed. No mismatch between confirmation screen and actual service deduction."
    },
    {
      "id": "T12",
      "ref": "R9",
      "severity": "MEDIUM",
      "title": "Fix bhatti edit audit trail and separate cost from selling price",
      "files": [
        "lib/services/bhatti_service.dart"
      ],
      "problem": "Bhatti edit reverses and reapplies stock without matching ledger records. Production cost is written into product.price, contaminating selling price.",
      "steps": [
        "Step 1: In editBhattiBatch(), before reversal, write explicit ledger entries of type 'bhatti_reversal' for all raw materials consumed.",
        "Step 2: After reapply, write ledger entries of type 'bhatti_reapply' for new raw material consumption.",
        "Step 3: Add a separate field product.productionCost (or a ProductCostRecord collection). Write production cost here, not into product.price.",
        "Step 4: Search all places that read product.price and verify they are not accidentally using production cost as selling price.",
        "Step 5: Write a test: edit a bhatti batch, verify ledger has reversal + reapply entries, verify product.price is unchanged."
      ],
      "acceptance_criteria": "Bhatti edit produces full ledger trail. product.price is only set by pricing workflow, never by production cost calculation."
    },
    {
      "id": "T13",
      "ref": "R15",
      "severity": "MEDIUM",
      "title": "Make dispatch and route-order status update atomic",
      "files": [
        "lib/screens/dispatch/dispatch_screen.dart",
        "lib/services/dispatch_service.dart"
      ],
      "problem": "Dispatch success and markOrderDispatched() are split. Dispatch can complete while route order stays pending.",
      "steps": [
        "Step 1: Create a new backend command 'dispatch_with_order_update' that includes both the dispatch payload and the routeOrderId to mark.",
        "Step 2: On server side, process both in a Firestore transaction: create dispatch doc AND update route order status to 'dispatched' atomically.",
        "Step 3: If atomic backend command is not feasible immediately, enqueue a follow-up outbox command 'mark_order_dispatched' immediately after dispatch command is enqueued. The processor will retry both until both succeed.",
        "Step 4: Write a test: simulate markOrderDispatched() failing. Verify the follow-up is retried and eventually both records are consistent."
      ],
      "acceptance_criteria": "After dispatch, route order status is always updated. There is no state where dispatch is completed but route order remains pending."
    },
    {
      "id": "T14",
      "ref": "R16",
      "severity": "MEDIUM",
      "title": "Fix dispatch movementIds map to handle duplicate product lines",
      "files": [
        "lib/services/inventory_service.dart"
      ],
      "problem": "movementIds map is keyed by productId. When same product appears twice in one dispatch, second entry overwrites first, losing one movement ID.",
      "steps": [
        "Step 1: Change movementIds map key from productId to lineItemIndex (integer or UUID per line).",
        "Step 2: Update all readers of movementIds to look up by line item index, not product ID.",
        "Step 3: Write a test: dispatch same product in two lines (e.g., paid + free). Verify movementIds map has two entries, not one."
      ],
      "acceptance_criteria": "Each dispatch line item has its own movement ID entry. No data loss for duplicate product lines."
    },
    {
      "id": "T15",
      "ref": "R12",
      "severity": "MEDIUM",
      "title": "Quarantine unauthorized pending product changes instead of silently syncing",
      "files": [
        "lib/services/delegates/inventory_sync_delegate.dart"
      ],
      "problem": "Unauthorized pending product changes are marked synced instead of being quarantined. Silent corruption is hidden from operators.",
      "steps": [
        "Step 1: In inventory_sync_delegate.dart, when a product change is rejected by server due to permissions, do NOT mark as synced.",
        "Step 2: Instead, move the record to a ConflictQueue collection with status='unauthorized' and timestamp.",
        "Step 3: Restore the local product record to its last known synced state.",
        "Step 4: Show a notification or badge in admin UI: 'X unauthorized changes were blocked and reverted.'",
        "Step 5: Write a test: restricted user creates local product change, syncs, verify product is reverted and appears in ConflictQueue."
      ],
      "acceptance_criteria": "Unauthorized changes are visible in a conflict queue. Local state is restored to server-authoritative state. No silent corruption."
    },
    {
      "id": "T16",
      "ref": "R13",
      "severity": "LOW",
      "title": "Fix retry expiry threshold to match configured maxAttempts",
      "files": [
        "lib/services/delegates/sync_queue_processor_delegate.dart",
        "lib/services/outbox_codec.dart"
      ],
      "problem": "Queue expiry expects >= 20 retries but default maxAttempts is 8. Auto-expiry never triggers.",
      "steps": [
        "Step 1: In outbox_codec.dart, define a constant: static const int maxAttempts = 8.",
        "Step 2: In sync_queue_processor_delegate.dart, replace hardcoded >= 20 with >= OutboxCodec.maxAttempts.",
        "Step 3: Write a test: exhaust maxAttempts retries, verify queue item is expired and moved to dead letter state."
      ],
      "acceptance_criteria": "Queue items are expired after exactly maxAttempts failures. One constant controls both the attempt limit and expiry threshold."
    },
    {
      "id": "T17",
      "ref": "R17",
      "severity": "MEDIUM",
      "title": "Eliminate N+1 reads in transaction and sync paths",
      "files": [
        "lib/services/inventory_service.dart",
        "lib/services/sales_service.dart",
        "lib/services/bhatti_service.dart"
      ],
      "problem": "Repeated per-item Isar and Firestore reads inside transactions. Department stock lookup scans aliases. Slow for large batches.",
      "steps": [
        "Step 1: In all transaction methods that loop over line items: collect all product IDs before the loop, fetch all products in one bulk Isar read, build a Map<productId, Product>.",
        "Step 2: Inside the loop, read from the map instead of querying per item.",
        "Step 3: For department stock lookups: replace alias scan with a direct keyed lookup using canonical department ID (after T9 is done).",
        "Step 4: For Firestore batch writes inside sync: use WriteBatch instead of individual document writes.",
        "Step 5: Write a performance test: process a 50-line sale. Verify total Isar read calls are O(1) not O(n)."
      ],
      "acceptance_criteria": "Large batch transactions complete without N+1 read patterns. Isar reads per transaction are bounded by a small constant regardless of line item count."
    }
  ],

  "final_verification_checklist": [
    "products.stock, departmentStocks, and stockLedger always agree after any operation",
    "All stock mutations have a corresponding ledger entry",
    "All remote writes go through the durable outbox queue",
    "Offline mutations are retried and eventually consistent with server",
    "No salesman stock allocation bypasses dispatch workflow",
    "Opening stock cannot be double-counted",
    "All collections use Firebase UID as canonical user key",
    "BOM violations are caught before stock is mutated",
    "Production writes require authenticated actor",
    "All department lookups use canonical ID from centralized master",
    "Unauthorized changes are quarantined, not silently hidden",
    "Queue expiry threshold matches maxAttempts constant",
    "Dispatch and route-order status update are atomic",
    "Duplicate product lines in dispatch preserve all movement IDs",
    "Unit tests pass for all 17 fix tasks"
  ],

  "execution_instructions": {
    "mode": "Implement fixes one task at a time in the order listed.",
    "after_each_task": "Run flutter test for affected files before moving to next task.",
    "branch_strategy": "Create one git branch per task: fix/T1-department-stock, fix/T2-salesman-block, etc.",
    "do_not": [
      "Batch multiple fixes into one commit",
      "Change unrelated code while fixing a task",
      "Skip writing the test for any task",
      "Mark a task done if its acceptance criteria are not fully met"
    ]
  }
}
